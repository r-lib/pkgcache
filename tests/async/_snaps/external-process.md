# cancel external_process

    Code
      synchronise(afun())
    Condition
      Error in `fun()`:
      ! failed

# can disable error on status

    Code
      synchronise(afun())
    Condition
      Error:
      ! process exited with non-zero status

